# This file is an example. For local development, copy it to a file named .env
# and replace the example URLs with your actual Netlify function URLs.

# The public URL to fetch the master list of sites (e.g., from your Netlify function).
SITES_JSON_URL="https://your-netlify-site-name.netlify.app/.netlify/functions/get_sites"

# The URL endpoint to submit a new site for registration.
SUBMIT_SITE_URL="https://your-netlify-site-name.netlify.app/.netlify/functions/submit_site"
